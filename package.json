{
    "devDependencies": {
        "@types/bun": "latest",
        "prisma": "latest"
    },
    "peerDependencies": {
        "typescript": "latest"
    },
    "private": true,
    "scripts": {
        "migrate": "prisma migrate dev",
        "migrate-init": "prisma migrate dev --name init"
    }
}
